{
  "compilerOptions": {
    "skipLibCheck": true,
    "strict": false,  // Optionally disable strict mode for even fewer checks
    "noEmit": false   // Ensure the build emits even if there are type errors
  }
}
